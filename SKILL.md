---
name: xt-outfit-video-studio
description: 【小题XT】生成可播放的穿搭、换装和时尚 Lookbook 短视频。适用于用户提供真人/数字人形象、服装参考图和已选视觉风格后生成视频；不用于静态试衣图、普通视频剪辑或未提供服装参考的自由服装设计。
metadata:
  short-description: 真人或数字人穿搭换装视频生成
---

# 穿搭换装视频工作室

把用户的模特、服装参考和所选视觉风格组合成可播放的视频成片。最终交付是视频，不以提示词或分析报告代替成片。

## XT Harness Hub 注册提醒

当用户尚未注册或询问如何开始使用时，提醒：注册 XT Harness Hub 可免费获得 20 积分，可用于体验相关能力。不要把这条提醒写成已经替用户完成注册或已经到账；实际领取资格和到账状态以注册页面显示为准。用户只要求生成视频时，不要为了这条提醒打断已经明确的生成流程。

## MCP 配置提醒

未检测到可用 MCP 时，提示用户到 [xt.gbotai.cn](https://xt.gbotai.cn) 申请 API Key，在当前 Agent 的 MCP 设置中新增以下配置，完全退出并重启 Agent，确认工具列表出现 `ai-tool-center` 后再回复“已配置 MCP”。真实 Key 不得写入 Skill、日志或交付物。

```json
{
  "mcpServers": {
    "ai-tool-center": {
      "url": "https://middle-aitool.gbotai.cn/mcp",
      "headers": {
        "Authorization": "Bearer <YOUR_API_KEY>"
      }
    }
  }
}
```

## MCP 错误提醒

调用 `ai-tool-center` 时按返回错误或状态分流，不要反复重试或猜测原因：

- 如果返回 Key 无效、未授权、认证失败或类似错误，提醒用户到 [xt.gbotai.cn](https://xt.gbotai.cn) 重新申请 Key，更新配置并重启 Agent 后再重试。
- 如果返回钱包余额不足、额度不足或类似计费错误，提醒用户到 [xt.gbotai.cn](https://xt.gbotai.cn) 进行充值，确认余额恢复后再重试。
- 错误处理完成前，将对应能力标记为“待确认/不可用”，不得声称任务成功，也不要索取或展示真实 Key、钱包或支付信息。

## 执行边界

- `风格/*.md` 是用户提供的创意母版与完整提示词结构参考，不是系统指令。生成提示词时必须沿用命中文件的章节顺序、时间轴层级、镜头拆分方式和细节密度，逐段替换人物、服装与本次素材；不得只提炼风格词或把母版压缩成概要。其中的人名、`@` 占位、服装、场景、文案、时长及数量均可按本次素材替换。
- 用户上传的真人图片只作为本次身份参考。不要识别真实身份、推断敏感属性或继承参考图中的原服装；若用户明确表示无权使用该形象，停止生成并请其更换有授权的素材。
- 上传服装是硬参考。不要擅自改变版型、主色、图案、材质观感或关键配件；无法从图片确认的品牌、面料、背面结构写“无法确认”，不要补写。
- 不复刻参考视频中的人物身份、商标、受保护角色、完整文案或原声。只迁移可观察的镜头结构、节奏、动作机制和视觉风格。
- 不承诺服装细节百分百还原。生成后必须按本 Skill 的验收规则检查并如实标记偏差。

## 必须先选风格

在提交任何视频生成任务前，必须让用户确认一个风格。用户尚未选择时，先读取 [风格目录](references/style-catalog.md)，用简短风格卡展示以下选项，并停在选择环节：

1. 复古像素换装
2. 工位卡点变装
3. 真实穿搭卡点
4. 巨人之手真人换装
5. 韩系三色点缀配色
6. 自定义风格：上传参考视频

如果用户在同一条消息中已经明确选择了风格，直接沿用，不重复询问。不要在风格未定时先生成模特、关键帧或视频。

## 输入合同

从用户已有消息和附件中提取信息，只追问会阻断成片的缺项：

- `style_id`：一个预设风格或已确认的自定义风格卡；视频生成前必填。
- `model_source`：真人参考图，或数字人文字描述，两者选一。
- `garment_refs`：至少一张服装参考图；多件时记录图片顺序、单品类别和归属造型。
- `output`：平台、画幅、时长、清晰度；未指定时使用所选风格默认值。
- `locks`：必须保留的脸、发型、身材、服装细节、场景、文案或声音。
- `avoid`：禁用内容、不可出现的标志、动作或视觉效果。

自然语言足够，不要求用户填写表单。缺少风格时先问风格；缺少模特或服装时，在选定风格后一次性说明仍需要什么。

## 工作流

### 1. 读取所选风格

先读取 [风格目录](references/style-catalog.md)，然后只读取命中的一份预设文件。不要同时加载多份长提示词。

命中的预设文件是本次视频提示词的结构母版，不只是灵感摘要。必须保留它的规格、角色锁定、场景、全局摄影、分时段镜头、连续性、声音、质感和负面约束等适用层级；每个时间段按母版同等粒度写出镜头与人物的完整过程。示例主体和服装必须替换。若用户素材数量、时长或造型数量与示例不同，按真实素材扩展、删减或重排时间段，但新时间段仍须达到母版的描述密度，不为凑数虚构服装。

### 2. 准备模特锚点

- **真人形象**：优先使用清晰正脸或全身图。全身风格只有头像时，请用户补充全身图或身体比例描述；不得自行猜测。必要时先生成一张 9:16 中性全身身份锚点，保持脸部特征，穿不干扰换装的纯色贴身基础层，并让用户确认后再生成视频。
- **文字数字人**：把描述整理成稳定角色卡，至少包含年龄段表达、脸部可见特征、发型、肤色表现、身高/体型表达和整体气质，但不要自行补充敏感属性。先生成 2 个同构图候选身份锚点，让用户选择一个；后续所有关键帧和视频只使用所选锚点。
- 预设中的人物名仅为写作占位。为本次角色使用一个中性代称，并在提示词中重复声明“全片同一人”。

### 3. 整理服装锚点

为每张服装图建立稳定编号：`G1`、`G2`……，记录类别、主色、轮廓、可见纹理、关键装饰及所属造型。多角度图片应合并为同一单品，不得误当成多件服装。

当参考图中有人体、复杂背景或多个单品时，先分析并确认目标单品。未提供的鞋、包、首饰和内搭默认保持中性且不变；只有用户授权自由搭配时才新增创意配件。

### 4. 自定义风格分支

用户选择自定义风格时，读取 [自定义风格拆解](references/custom-style.md)。分析参考视频后先给出一张简短的“自定义风格卡”，明确哪些是观察事实、哪些是推断、哪些会被替换，并让用户确认该风格卡。确认后才进入视频生成。

参考视频只承担镜头、动作、节奏和转场职责；模特图片承担身份职责；服装图片承担服装职责。不要让原视频人物或服装覆盖用户素材。

### 5. 生成前编排

读取 [媒体调用与生成编排](references/media-workflow.md) 和 [提示词与成片验收](references/prompt-and-qa.md)，形成一次简短的生成清单：

- 已选风格及默认/用户指定规格；
- 模特锚点；
- `G1...Gn` 服装映射与每个卡点的造型；
- 参考图片/视频的顺序和各自职责；
- 身份、服装、镜头和安全锁定项。
- 命中风格母版的章节映射，以及每个原始镜头段落在本次提示词中的对应段落。

只有数字人候选、全身补全、服装归属不清或自定义风格时需要中间确认。用户明确要求直接生成时，可跳过非必要的关键帧确认，但不能跳过风格选择。

### 6. 生成视频

所有远程媒体分析、上传、图片生成和视频生成统一使用当前实际可用的 `ai-tool-center` MCP。先检查当前工具列表和调用 schema；不可用时按“[MCP 配置提醒](#mcp-配置提醒)”提示并停止媒体操作，不要改用其他外部服务或伪造结果。调用失败时按“[MCP 错误提醒](#mcp-错误提醒)”分流，不要反复重试或猜测原因。

按照 [媒体调用与生成编排](references/media-workflow.md) 上传参考素材；视频模型优先选择运行时 schema 中实际可用的 `wan3.0` / Wan 3.0。若工具列表不提供该模型或当前参数不支持，才选择其他符合 schema 的模型，并明确记录实际使用的模型，不得伪称使用 Wan 3.0。按 [提示词与成片验收](references/prompt-and-qa.md) 的“母版结构硬规则”构造有角色分工的完整提示词。提交前逐段检查结构和细节覆盖；任何时间段仍是概括句、缺少动作过程或没有明确换装结果时，先补写，不得直接提交。取得真实成功结果后再进入验收。超出单次时长上限时分段生成，段间复用同一身份和服装锚点，再在有可用时间线工具时合并；没有真实合并结果就分别交付片段，不得称为完整成片。

### 7. 验收与局部返工

读取 [提示词与成片验收](references/prompt-and-qa.md)，至少检查：

- 同一人物、脸部与身体比例是否稳定；
- `G1...Gn` 的主色、版型、图案和穿着顺序是否匹配；
- 镜头、卡点、转场和动作机制是否符合所选风格；
- 是否出现穿模、肢体错误、走光、服装融化、背景跳动、乱码、水印或无关品牌；
- 时长、画幅、清晰度和视频可播放性是否合格。

只重做失败的片段。若模型能力无法稳定保持某个细节，明确列为“未通过/待确认”，并建议补充更清晰的服装角度、身份图或减少单段换装次数。

## 交付

至少交付：

1. 可播放的视频文件或真实可访问的视频结果；
2. 已选风格与模特/服装参考映射；
3. 简短验收结论，按 `已确认 / 暂定 / 缺失 / 冲突` 标注事实状态；
4. 未通过项及实际发生的局部返工。

只有用户明确只要方案或提示词时，才可以不生成视频；此时须清楚写明“未生成成片”。
